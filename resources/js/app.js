/* new Vue({
    el: "#app",
    data: {
        skills: [],
    },
    created() {
        // make an ajax request to our server
        // fetch()
        // axios - library
        axios.get("/skills").then((response) => (this.skills = response.data));
    },
});
 */
